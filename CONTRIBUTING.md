# Contributing

Open an issue before large architectural changes. Keep pull requests focused and include a reproducible test or trace when changing estimator behavior.

For backend changes, run `make test`. For Android changes, state the Android version and handset used for validation. Do not add captured BSSIDs, cell identifiers, precise personal locations or secrets to fixtures.
